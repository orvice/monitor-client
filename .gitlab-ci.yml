variables:
  IMAGE_NAME: $CI_BUILD_RG/orvice/${CI_PROJECT_NAME}

image: docker:latest
services:
  - docker:dind
stages:
  - test
  - build
  - deploy

include:
  - project: 'ops-public/ci-templates'
    file: 'build/go-docker.yml'
  - project: 'ops-public/ci-templates'
    file: 'build/go-bin.yml'